VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# DeepSeek API Key for AI speaking review (set via `supabase secrets set DEEPSEEK_API_KEY=sk-xxx`)
# This is NOT a VITE_ variable — it stays server-side in Supabase Edge Functions, never exposed to client.
